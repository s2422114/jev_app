# CLAUDE.md

## このプロジェクトについて
- Jev（TypeSafe AI の System One モデル。文章は生成せず、型付きの判断 Noul / Choice / Score を確率・confidence つきで返す）を使ったアプリ。
- 題材：株の自動投資の判断を Jev に担わせる。
- 目的は2つ。(1) Jev の新しいユースケースの開拓、(2) 就活用ポートフォリオ。デプロイして外部公開し、ポートフォリオから触れる状態にする。
- 学習目的のプロジェクト。優先するのはユーザー本人の理解であり、完成の速さではない。
- 要件定義はまだしていない。このファイルが定めるのは「進め方」だけ。要件と技術選定は各フェーズでユーザーが決める。

## やり取りのルール（最優先）
1. 返答は必要最低限にする。聞かれたことにだけ答える。聞かれていない解説、代替案の列挙、長いまとめは書かない。
2. 最初から正解（完成形の設計やコード一式）を出さない。ユーザーが指示した単位で小さく進める。わからないことはユーザーが都度聞く。
3. 本番（フェーズ2以降）で Jev に渡す質問文、state の形、閾値、回答の合成ロジックはユーザーが考える。頼まれたときだけ叩き台を出す。
  ただしフェーズ1の実験は下の「フェーズ1の分担」に従う。
4. わからないことは「わからない」と言う。推測で埋めない。
   - Jev は 2026-09-15 に early access で公開されたばかりで、学習データに含まれていない前提で扱う。API のフィールド名、SDK の関数名、挙動を記憶や推測で書かない。下の「Jev の情報源」で確認し、確認できなければそう伝える。
   - 確認した事実と推測は区別して伝える。
5. 要件や技術選定を勝手に確定しない。決める必要が出たときだけ、選択肢を短く示してユーザーに決めてもらう。

## フェーズ1の分担
実験の一覧と記録のルールは `notes/experiments-plan.md` にある。

- ユーザーがやること：実験の選択、各ケースの予想、C（質問の書き方）の実験の質問文、結果の解釈。
- Claude Code がやること：
  - 材料ファイル（架空の銘柄、数値、文章）を作る。
  - C 以外の実験の state と質問文を組み立てる。
  - 実験を実行し、結果と usage をファイルに記録する。
  - レポートのテンプレートを1回だけ作る。以後はユーザーがそれを使って書く。
- 実験用に書いた state と質問文は、本番にそのまま流用しない。

## 決まっていること / いないこと
- 方向性の決定事項と未決の論点は `notes/decisions.md` に、jev-1.13 の苦手分野は `notes/jev-weaknesses.md` にある。作業を始める前に両方読むこと。決定は notes/ 側が優先。
- 株価データはデモデータを使う。実データでの運用や実取引はしない。取得・作成方法は未決定で、作業時に決める。
- 言語、フレームワーク、デプロイ先は未決定。（決定済みのため、notes/decisions.md を参照）
- ポートフォリオ用にリポジトリを公開する可能性がある。API キーは .env に置き、最初のコミットより前に .gitignore に入れる。キーをコードやチャットに書かない。
- ハーネス（LangChain / Pydantic AI などの Jev 連携）やスキルは、プロダクトが良くなる見込みがあるときに理由を1〜2行添えて提案する。採否はユーザーが決める。
- TypeSafe 公式の agent skill はフェーズ0で導入する（Jev の API を推測で書かないため）。
  - `claude plugin marketplace add typesafe-ai/skills`
  - `claude plugin install typesafe@typesafe-ai`

## Jev を使うときの設計原則（公式ガイドの要点）
- 制御フロー、決定論的な処理、副作用（発注など）はコードが持つ。Jev は狭い判断だけを担当する。
- 数値計算、件数カウント、日付や時刻の比較は Jev にやらせない（jev-1.13 の既知の弱点）。指標の計算はコードで行い、計算済みの値か意味のあるラベルにして渡す。
- 「買うべきか」のような広い質問は、1つの性質だけを見る原子的な質問に分解する。独立した質問は1リクエストにまとめて聞く（並列に評価される）。
- state には、その質問に必要な情報だけを入れる。
- 回答の合成（重み付けやルール）はコードで行う。
- 確率と confidence で分岐する：自動で実行 / 見送り / 人の確認に回す。
- 質問文と閾値の定数は1ファイルに集約する（人がレビューしやすくするため）。
- 閾値を調整したら、モデル指定はエイリアス（jev-latest）ではなくバージョン ID で固定する。
- 精度が最も高いのは英語。日本語の state や質問を使う場合は先に精度を試す。

## 進め方（フェーズ）
各フェーズの終わりに「わかったこと」を notes/ に短く残す（ユースケース開拓とポートフォリオの材料になる）。次のフェーズに進むかはユーザーが決める。

0. 準備：API キーの取得、Playground で手で触る、公式 docs を読む、TypeSafe skill の導入。公式の use-case map と cookbooks を見て、既出の使い方と被らない切り口を確認する。
1. 素振り：アプリは作らず、SDK から株ドメインの小さな state で Noul / Choice / Score を叩く。Jev が何を判断できて何が苦手かを体感する（例：生の数値を渡す場合と、コードで加工して渡す場合の比較）。
2. 判断の設計：売買判断を原子的な質問に分解し、コードでの合成と confidence による分岐の形を決める。
3. 縦切り：デモデータ → 判断 → 仮想売買 → 結果表示 を最小構成で1本通す。通ったら一度デプロイして公開まで通す（デプロイを最後に回さない）。
4. 評価：デモデータで回し、confidence と結果の関係を見て閾値を調整する。Jev を使わないルールだけの場合と比べ、Jev を入れた意味があるかを確認する。
5. 公開の仕上げ：API キーはサーバー側に置く。公開エンドポイントの呼び出し制限やキャッシュ。「デモデータであり投資助言ではない」旨の表示。
6. ポートフォリオ化：README、構成図、Jev の得手不得手についての学び、デモ URL。

任意：LLM の併用やハーネスの導入（例：判断は Jev、説明文の生成は LLM）。必要性が見えた時点で検討する。

## Jev の情報源（仕様はここで確認する）
- docs の索引：https://docs.typesafe.ai/llms.txt
- 作り方：https://docs.typesafe.ai/concepts/how-to-build-with-system-one
- 既知の弱点（jev-1.13）：https://docs.typesafe.ai/model-jaggedness/jev-1.13
- パターン：https://docs.typesafe.ai/patterns
- confidence の扱い：https://docs.typesafe.ai/confidence
- モデル・料金・制限：https://docs.typesafe.ai/models
- Agent skill：https://docs.typesafe.ai/agent-skill
- 題材が近い公式例（取引リクエストを関数呼び出しに変換）：https://docs.typesafe.ai/cookbooks/function_calling
